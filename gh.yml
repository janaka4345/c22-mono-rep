version: "3.3"
services:
  # web:
  #   image: nginx:latest
  #   ports:
  #     - "8080:80"
    # volumes:
    #   - ./nginx.conf:/etc/nginx/nginx.conf
    # depends_on:
    #   - app
  # app:
  #   image: node:latest
  #   volumes:
  #     - .:/app
  #   working_dir: /app
  #   command: npm run dev
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=development
  #   depends_on:
  #     - db
  # db:
  #   image: mysql:5.7
  #   environment:
  #     MYSQL_ROOT_PASSWORD: root
  #     MYSQL_DATABASE: mydb
  #     MYSQL_USER: user
  #     MYSQL_PASSWORD: password
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - ./mysql:/var/lib/mysql